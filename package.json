{
	"name": "@tradercore/plugin-template",
	"private": true,
	"scripts": {
		"build": "pnpm -r build",
		"dev": "pnpm -r dev",
		"clean": "pnpm -r clean",
		"test": "pnpm -r test"
	},
	"engines": {
		"node": ">=18",
		"pnpm": ">=8"
	}
}
